# Build / runtime artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
.python-version
.venv/
venv/

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local test artifacts (the test runner writes here on demand)
$TEMP/vscode-skill-tests/

# Local config / secrets (never commit user-specific config)
config.local.json
*.local.json

# Output of running tests (local repro helpers)
test-summary.json